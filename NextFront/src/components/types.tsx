export interface DataType {
    id: string;
    match: string;
    odds: string[];
    newTextField: string;
    trj: string;
  };

export interface ValueTableInputs {
    issuesNumber: number;
    betNumber: number;
}